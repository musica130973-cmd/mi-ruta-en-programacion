// Diferencia entre null y undefined

console.log(null == undefined);
console.log(null === undefined);

// null == undefined: true Porque == considera como "vacios" y los trata como equivalentes
// null === undefined: false Porque === exige mismo tipo de valor, y sus tipos son distintos