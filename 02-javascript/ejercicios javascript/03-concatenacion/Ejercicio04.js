// Ejercicio 4
// Crea una URL dinámica
// Dado un dominio y una ruta, construye una URL completa concatenando ambos.

let dominio = "https://midominio.com/";
let ruta = "productos";

let url = dominio + ruta;
console.log(url);
